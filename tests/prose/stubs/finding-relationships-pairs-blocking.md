# stub: finding-relationships-pairs-blocking

The relationships agent's groups for the set: the payment-intent task's
blocking entry and its finding name one defect at one site — the entry
records the unmet criterion, the finding prescribes its remedy — so they
are one overlap group; the capture finding targets a different file and shares no
binding. Write the JSON below to the output path the dispatch names.

---
{"groups":[{"kind":"overlap","ids":["1-1-b1","1-1-1"],"files":["src/checkout/payment-intent.js"],"merged_intent":"fix the intent's methods to ['card'] so card-only holds whatever the order carries","note":"the blocking entry names the unmet criterion; the finding prescribes the one-line remedy"}]}
