# stub: finding-assessed-assertion

The assessor's verdict for the batch: the one finding holds against the
code — the intent test does assert back the literal it built — and its
proposed change touches no comment or documentation prose. Write the line
below to the output path the dispatch names, one JSON object per line and
nothing else. The counts are also what the agent returns.

---
{"id":"1-payment-intent-1","valid":"valid","standard":"n/a","rule":"-","remedy":"-","note":"the assertion reads back the literal the test itself built"}
