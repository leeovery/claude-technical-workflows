# stub: finding-guards-checked-assertion

The guards agent's verdict for the batch: this project asserts no invariant
the finding could breach. Write the line below to the output path the
dispatch names, one JSON object per line and nothing else. The inventory is
also what the agent returns to its caller.

---
{"id":"1-payment-intent-1","guard":"none","which":"-","how":"assertion rewrite, no invariant at stake"}
